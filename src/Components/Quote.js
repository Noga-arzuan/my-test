/*import React from 'react';
const Quote = (props)=>{
    console.log(props.quote)
    return (
        <div>
            <card>
            <ol>{props.quote.map(text=>(
            <li>{text.text}</li> 
            ))}
            </ol>
          
           
            </card>
        </div>
    );
}
export default Quote;*/